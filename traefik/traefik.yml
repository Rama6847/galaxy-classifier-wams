# traefik.yml
# traefik.yml
api:
  dashboard: true
  debug: true
  insecure: true   # ← AJOUTE CETTE LIGNE

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: "/etc/traefik/dynamic.yml"
    watch: true